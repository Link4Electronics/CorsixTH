name: Linux AppImage

on:
  push:
    branches-ignore: [ 'gh-pages' ]
  pull_request:
  workflow_dispatch:
    inputs:
      pr:
        description: 'Build this PR (optional)'
        type: number

jobs:
  build:
    name: Build ${{ matrix.arch }}
    strategy:
      matrix:
        include:
          - arch: x86_64
            runs-on: ubuntu-latest
          - arch: aarch64
            runs-on: ubuntu-24.04-arm
    runs-on: ${{ matrix.runs-on }}
    container: ghcr.io/pkgforge-dev/archlinux:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup pkgforge-dev environment
        uses: pkgforge-dev/anylinux-setup-action@v2
      - name: Install Build Dependencies
        run: |
          # Pulling dependencies based on CorsixTH Arch requirements
          pacman -Syu --noconfirm cmake ninja gcc lua lua-filesystem lua-lpeg \
          sdl2 sdl2_mixer freetype2 ffmpeg doxygen fluidsynth libdecor rtmidi
          get-debloated-pkgs --add-common --prefer-nano ffmpeg-mini
      - name: Checkout PR (Optional)
        if: github.event.inputs.pr
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh pr checkout ${{ github.event.inputs.pr }}
      - name: Build CorsixTH
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=RelWithDebInfo \
            -DENABLE_UNIT_TESTS=OFF \
            -DCMAKE_INSTALL_PREFIX=/usr
          cmake --build build --parallel $(nproc)
          DESTDIR=./pkg_root cmake --install build
      - name: Generate AppImage
        env:
          OUTPATH: ./dist
          # Set the path to your binary and essential assets
          APP_BIN: ./pkg_root/usr/bin/corsix-th
          ICON: ./pkg_root/usr/share/icons/hicolor/scalable/apps/corsix-th.svg
          DESKTOP: ./pkg_root/usr/share/applications/corsix-th.desktop
          DEPLOY_OPENGL: 1
          DEPLOY_PIPEWIRE: 1
        run: |
          # Use quick-sharun to bundle and create the AppImage
          quick-sharun "$APP_BIN" ./pkg_root/usr/lib/lua/*/lpeg.so ./pkg_root/usr/lib/alsa-lib ./pkg_root/usr/lib/libpulse-simple.so* ./pkg_root/usr/lib/libfluidsynth.so*
          cp -v /etc/timidity/timidity.cfg ./AppDir/bin
          echo 'SHARUN_WORKING_DIR=${SHARUN_DIR}/bin' >> ./AppDir/.env
          mkdir -p ./AppDir/share/soundfonts
          wget https://raw.githubusercontent.com/Jacalz/fluid-soundfont/master/SF3/FluidR3.sf3 -O ./AppDir/share/soundfonts/FluidR3.sf3
          quick-sharun --make-appimage
      - name: Upload AppImage
        uses: actions/upload-artifact@v4
        with:
          name: CorsixTH-AppImage
          path: dist/*.AppImage
